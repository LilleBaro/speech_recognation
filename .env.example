# Copy to .env and fill in as needed. docker-compose reads this file automatically.

# Optional: avoids Hugging Face Hub rate limits on repeated model downloads.
HF_TOKEN=

# Overrides for the sentiment model source (defaults match models/pipeline.py).
# HF_REPO_ID=LilleBaro/DistilBert_sentiment_analysis
# HF_MODEL_NAME=distilbert-base-multilingual-cased
